/**
 * @deprecated This component is replaced by VictoryScreen and DefeatScreen
 * Kept for backward compatibility but returns null
 */
export function GameOverScreen() {
  // VictoryScreen handles 'victory' state
  // DefeatScreen handles 'starvation' and 'defeat' states
  // This legacy screen is no longer needed
  return null;
}
